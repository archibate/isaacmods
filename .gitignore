# Cloned reference repos — managed externally, not part of workspace source
/isaac-lua-api/
/isaac-docs/

# Session memory (remember skill)
/.remember/

# Bytecode dropped by `luac` when it is asked to compile rather than just check
/luac.out
