- Fixed the controller fast-restart button ignoring the FastRestartEnable switch: turning it off only disabled the R key, so a bound ControllerAlternateR still restarted the run — including while paging item descriptions with EID (thanks @TesKwokin for the report!)
