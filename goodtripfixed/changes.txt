- Fixed settings never being saved when Mod Config Menu is not installed: every setting was written from inside the menu's own code, so without it nothing ever reached the disk — the map position, the zoom level and a map dropped in the trash now all stick across launches, and no longer wait for a clean quit (thanks @空崎ヒナ and @时佚 for the reports!)
- Fixed a room you had already cleared being refused as a target when it had no cleared room next to it, which made FairTripPath look like it did nothing: the uncleared-neighbor rule had replaced the original "visited and cleared" rule instead of widening it, so after an Emperor card jump there was no way back to the start room with the switch either on or off (thanks @HeavenUsurper for the report!)
