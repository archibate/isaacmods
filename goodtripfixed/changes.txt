- Fixed teleport crashing on some game builds with "bad argument to StartRoomTransition (number expected, got boolean)" — a legacy duplicate transition call passed the TeleportAnimation boolean as the animation id; strict builds rejected it and the teleport silently died (thanks @校医院也有B超 for the console log!)
